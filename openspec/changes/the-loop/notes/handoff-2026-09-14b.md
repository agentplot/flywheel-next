# Handoff, 2026-09-14 (night)

Written for the agent picking this up. Read `tasks.md` group 20 first; every
row of it is ticked.

## What stands

The slice runs on flywheel-next itself. An instance at `~/flywheel/agentplot.yaml`
tracks `flywheel-next` (mirror at `~/flywheel/git-host/agentplot-flywheel-next.git`),
with `workspace: host` and `sessions: herdr`. Its first bolt,
`bolt/flywheel-next/readme-crates-table`, went from a capture typed on the
page to a chore unit, a Claude Code session in a Herdr pane at a real
worktree, a commit, `exit done`, the merge, the close answered on the page,
and a landing on the mirror's `main` — pulled into this checkout as d63b6f7.
README's "Run the loop on your laptop" is that walk, and
`crates/flywheel/tests/system/walkthrough.rs` plays it with the operator as
the session.

`scenarios/storefront` runs through a landed bolt, stepped from the page:
`scripts/tour-walk.sh` applies it, serves it, clicks `next` nineteen times
and screenshots every step (`target/shots/tour/`).

The toolchain is devenv's (`devenv shell -- unit|integration|system`); every
cargo command runs inside it. Do not run the integration tier while building;
the pre-merge hook runs it. The timings in AGENTS.md are measured.

## What the live run showed and how it was met

- The work order names the binary by full path; a pane's shell has no
  `flywheel` on its path.
- `flywheel init` defaults the address to `http://<hostname>.local`; a host
  refuses a localhost address.
- Claude Code asks whether the folder is trusted before it takes a prompt.
  `Herdr::clear_the_way` answers it from what the pane shows and waits for
  the prompt line; the first prompt follows.

## The page, as it stands tonight

- The rail and the board are one selection (S219): the decision in hand is
  lit and so is the board object it stands on (`data-board` on the card;
  `board_object()` in `page.rs` walks to the nearest drawn parent, so a
  signal's decision lights its capture). `j`/`k`, Enter/`o`, the marks on
  board objects and a click on a board object all move it. The script is in
  `page/template.html`; nothing is fetched from anywhere but the host.
- A decision is a sentence with labelled, keyed controls (S220):
  `page/asks.rs` holds the question per kind, the label per answer, the
  tooltip line, the primary/dismiss tone and the key assignment
  (`data-key`, first free letter, never `j k o /`). The drawer foot uses the
  same `control()`.
- Live refresh (S221): `Served.changed` is a watch channel; the loop bumps it
  after a pass that moved (`main.rs`), `GET /events` streams it as SSE, the
  page fetches itself and swaps regions by id (`REGIONS` in the script).
  `POST /api/wake` wakes the loop; `flywheel exit|note|…` calls it through
  `FLYWHEEL_PAGE`, which the serving host puts in the work order.
  `crates/flywheel/src/watch.rs` keeps one blocking `herdr agent wait` per
  live agent (`Herdr::wait_change`, `live_agents`) and bumps both on change.
  Verified end to end over CDP: a capture posted from outside appeared on an
  open page in under four seconds with the hand and the lit object kept.
- Words: *branch* everywhere on the page (S222); a typed capture is *from the
  console* (S223); the hidden `source` field of the capture form is
  `console`.

Still to do on the page:

- The grey guard strings under headings and on the ledger foot (`landed ·
  line removed · place removed · services gone`) come from `status.rs said()`
  and want one sentence per machine. Captured on the live instance as
  decision 3, so the loop can build it.
- Selection inside a drawn object (S219's last sentence): the units of a
  bolt, the beads of an intent.
- The landed bolt's commit list is main's latest (the branch holds nothing
  main does not); the section says so. A landed bolt should keep the
  commits it carried, which means recording the merge commit at landing.
- Intent and elaboration dock pages are still plain; the landed-bolt page
  (S28) is not built.

## Left as the model has it, worth a look

- A session's pane outlives its exit: `end_session` runs only from `ended`,
  which the owner's explicit end reaches (`session.yaml`), so a done agent's
  pane stays in Herdr until closed by hand. Whether a passed stage should end
  it is a model question.
- `write_acceptance` on the host workspace writes an empty file under
  `flywheel/acceptance/`; the recorded binding's body is kept. What the
  acceptance should carry is a model question.
- A tracked repository adopted by init gains the template commit
  (`flywheel/.keep`).
- The `unit…` control is offered on every capture, moved or not.
- `place_key` gives a session's place as the object id and the place's own
  region as `<object>#own`; `place_dir_of` in host.rs tries both. The model
  should say which.
- A place's `devenv.nix` is not activated before the session starts
  (`host.yaml` says it is); the agent runs in the operator's shell.

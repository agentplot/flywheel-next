//! The acceptance table, as the tests read it: the runner's own list, so the
//! gate and the runner cannot disagree about what the phase accepts.

#![allow(unused_imports)]

pub use flywheel_scenario::conformance::phase::{accepted, ACCEPTED, DEFERRED, REAL_HOSTS, REAL_WORKSPACE};

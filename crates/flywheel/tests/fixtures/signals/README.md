# Signals

A fixture in the layout a blueprints' `signals/` folder is written in: one
directory per capture, a `capture.md` and one markdown file per signal. The
material is invented; no client's words are in it.

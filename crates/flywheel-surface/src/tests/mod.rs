//! The surface's own tests: the catalogue's calls, the bodies a tool answers
//! with, the dictation path and the links the machinery writes — all over the
//! fake store and the fake world, because none of them is about a repository.
//! The first tier (D17).

mod bodies;
mod chat;
mod dictation;
mod links;
